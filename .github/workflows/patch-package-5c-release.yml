name: Patch Paket 5C release

on:
  push:
    branches:
      - agent/paket-5c-besoksfoto
    paths:
      - .github/workflows/patch-package-5c-release.yml
      - scripts/patch-package-5c-release.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: agent/paket-5c-besoksfoto
          fetch-depth: 0

      - name: Apply release patch
        run: python3 scripts/patch-package-5c-release.py

      - name: Commit release files
        run: |
          git config user.name "Matrundan automation"
          git config user.email "actions@users.noreply.github.com"
          git add src/lib/matrundan/version.ts README.md CHANGELOG.md docs/architecture.md
          git diff --cached --quiet && exit 0
          git commit -m "Paket 5C: synka release och arkitektur för 0.14.0"
          git push origin HEAD:agent/paket-5c-besoksfoto
