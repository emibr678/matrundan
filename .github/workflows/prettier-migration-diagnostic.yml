name: Prettier migration diagnostic

on:
  pull_request:
    branches: [main]

jobs:
  format:
    if: github.event.pull_request.number == 222
    runs-on: matrundan-self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - uses: ./.github/actions/setup-bun
      - run: bun install --frozen-lockfile
      - name: Print exact Prettier output
        shell: bash
        run: |
          bunx prettier --write scripts/migrate-visit-photos.mjs
          echo '---BEGIN_FORMATTED_FILE---'
          cat scripts/migrate-visit-photos.mjs
          echo '---END_FORMATTED_FILE---'
