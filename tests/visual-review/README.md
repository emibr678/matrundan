# Visual review tests

Kör standardmatrisen lokalt med:

```bash
bun run test:visual-review
```

Ange egna komma- eller radseparerade demosökvägar med `VISUAL_REVIEW_PATHS`.
Skärmbilder skrivs till den git-ignorerade katalogen `visual-review/`.
